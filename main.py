seconds_per_minute = 60
minutes_per_hour = 60
hours_per_day = 24

# Formula for seconds per hour.
seconds_per_hour = 
seconds_per_day = 
print("Seconds per day:", seconds_per_day)

# Compare / and //.
print (s / d * h / s = hs / ds = h / d )
print("", seconds_per_day / seconds_per_hour, "\n")
print(seconds_per_day // seconds_per_hour)